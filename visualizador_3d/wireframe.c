#include <SDL2/SDL.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "datatypes.h"

int main(int argc, char** argv){

    if (argc > 1){
        fprintf(stderr, "Uso: %s <arquivo.obj>\n", argv[0]);
        exit(1);
    }
    
    if (argc == 1){
    
    }
    else{
            
    }

    return 0;
}
